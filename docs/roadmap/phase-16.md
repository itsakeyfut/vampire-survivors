# Phase 16: エフェクト・ポリッシュ

## フェーズ概要

**ステータス**: 🔲 未着手
**推定工数**: 6-8時間
**依存関係**: Phase 15

### 目的
視覚エフェクト・ダメージ数値・UI演出を追加してゲームをポリッシュする。ゲームバランスの微調整とバグ修正も行う。

---

## タスクリスト

### タスク 16.1: ダメージ数値フロートテキスト

**優先度**: P1
**推定工数**: 1.5時間
**ラベル**: task, phase-16, polish

**説明**:
敵がダメージを受けた時、ダメージ数値が敵の上に浮かび上がってフェードアウトするエフェクトを実装する。

**受け入れ基準**:
- [ ] 敵ダメージ時に数値テキストが出現する
- [ ] 1秒かけて上に移動しながらフェードアウトする
- [ ] 通常ダメージは白、クリティカルは黄（将来拡張）
- [ ] 多数の数値が同時に表示されてもパフォーマンスに影響ない

**実装ガイド**:
```rust
#[derive(Component)]
pub struct DamageText {
    pub lifetime: f32,
    pub velocity: Vec2,
}

pub fn spawn_damage_text(
    commands: &mut Commands,
    position: Vec2,
    damage: f32,
    font: &FontAssets,
) {
    commands.spawn((
        DamageText {
            lifetime: 1.0,
            velocity: Vec2::new(0.0, 50.0),
        },
        Text2d::new(format!("{:.0}", damage)),
        // テキストスタイル設定
        Transform::from_translation(position.extend(20.0)),
    ));
}

pub fn update_damage_texts(
    mut commands: Commands,
    mut query: Query<(Entity, &mut DamageText, &mut Transform, &mut TextColor)>,
    time: Res<Time>,
) {
    for (entity, mut text, mut transform, mut color) in query.iter_mut() {
        text.lifetime -= time.delta_secs();
        transform.translation += (text.velocity * time.delta_secs()).extend(0.0);

        let alpha = text.lifetime.max(0.0);
        color.0 = color.0.with_alpha(alpha);

        if text.lifetime <= 0.0 {
            commands.entity(entity).despawn();
        }
    }
}
```

---

### タスク 16.2: 敵被ダメージエフェクト（点滅）

**優先度**: P1
**推定工数**: 0.5時間
**ラベル**: task, phase-16, polish

**説明**:
敵がダメージを受けた際に赤く点滅するエフェクトを実装する。

**受け入れ基準**:
- [ ] ダメージ時にスプライトが赤くなる
- [ ] 0.1〜0.2秒後に元の色に戻る
- [ ] `DamageFlash` コンポーネントで管理する

---

### タスク 16.3: XPジェム吸収エフェクト

**優先度**: P2
**推定工数**: 0.5時間
**ラベル**: task, phase-16, polish

**説明**:
XPジェムがプレイヤーに吸収される際に軌跡エフェクトを追加する。

**受け入れ基準**:
- [ ] 吸収範囲内に入ったジェムが光りながらプレイヤーに向かって移動する
- [ ] 吸収時に短い光エフェクトが出る

---

### タスク 16.4: レベルアップエフェクト

**優先度**: P1
**推定工数**: 0.5時間
**ラベル**: task, phase-16, polish

**説明**:
レベルアップ時にプレイヤー周囲に光が広がるエフェクトを実装する。

**受け入れ基準**:
- [ ] レベルアップ時に円形の光エフェクトが一瞬広がって消える
- [ ] 選択カードが飛び込んでくるようなアニメーション

---

### タスク 16.5: 武器進化エフェクト

**優先度**: P1
**推定工数**: 1時間
**ラベル**: task, phase-16, polish

**説明**:
武器進化時に画面全体に派手なエフェクトを表示する。

**受け入れ基準**:
- [ ] 宝箱開封で進化発動時、画面が一瞬明るく光る
- [ ] 「EVOLVED!」テキストが中央に表示されてフェードアウト
- [ ] 進化後の武器のエフェクトが明らかに変化する

---

### タスク 16.6: ボス出現演出

**優先度**: P1
**推定工数**: 1時間
**ラベル**: task, phase-16, polish

**説明**:
ボス出現時に画面を揺らし、警告テキストを表示する演出を実装する。

**受け入れ基準**:
- [ ] 30分経過時に「WARNING」「A POWERFUL ENEMY APPROACHES」等の警告テキスト
- [ ] 短い画面振動（カメラシェイク）
- [ ] ボスが画面外から中央に向かって入場するアニメーション

---

### タスク 16.7: ゲームバランスの調整

**優先度**: P0
**推定工数**: 2時間
**ラベル**: task, phase-16, balance

**説明**:
実際にプレイして気になったゲームバランスを調整する。

**調整対象**:
- [ ] 敵のHP・速度・ダメージの難易度曲線
- [ ] 武器ダメージと敵HPのバランス（30分でちょうどよい難易度）
- [ ] XP量とレベルアップ頻度（序盤で早めに、後半は稀に）
- [ ] スポーン間隔のスケーリング
- [ ] ボスのHP・フェーズ移行タイミング

**受け入れ基準**:
- [ ] 初心者がMVP武器（ムチ）だけで10分は生存できる難易度
- [ ] 30分の生存が適切な挑戦として機能する

---

### タスク 16.8: パフォーマンス最終確認

**優先度**: P0
**推定工数**: 0.5時間
**ラベル**: task, phase-16, performance

**説明**:
エフェクト追加後も60fps以上を維持できることを確認する。

**受け入れ基準**:
- [ ] 300体の敵 + エフェクト全有効の状態で60fps以上を維持する
- [ ] リリースビルドで安定したパフォーマンス

---

## フェーズ検証

### 検証項目
- [ ] ダメージ数値がキャラクター上に表示される
- [ ] 敵が赤く点滅してダメージを受けたことが分かる
- [ ] レベルアップ時に視覚的な演出がある
- [ ] 武器進化時に派手なエフェクトがある
- [ ] ボス出現時に緊張感のある演出がある
- [ ] 30分プレイして適切な難易度と感じる
- [ ] 300体 + エフェクトで60fps以上

## 次のフェーズ

Phase 16 完了 → ポリッシュ版 (M3) 達成！次は **Phase 17: ピクセルアート統合**（後日）
