# Phase 1: プロジェクトセットアップ

## フェーズ概要

**ステータス**: 🔲 未着手
**推定工数**: 2-3時間
**依存関係**: なし

### 目的
Cargoワークスペース・開発環境・設計ドキュメントを整備し、空のウィンドウが開く状態にする。

### スコープ
- 4クレート構成のワークスペース設定
- 全設計ドキュメントの作成（このロードマップの作成で完了）
- 開発環境の整備（justfile・.gitignore等）
- 基本的な main.rs の作成

---

## 前提条件

- Rust (stable channel) がインストールされている
- Cargo がインストールされている
- Git がインストールされている
- Just コマンドランナーがインストールされている（推奨）

---

## タスクリスト

### タスク 1.1: Cargoワークスペースの設定

**優先度**: P0
**推定工数**: 1時間
**ラベル**: task, phase-1

**説明**:
4クレート構成のCargoワークスペースを設定し、各クレートの基本構造を作成する。

**受け入れ基準**:
- [ ] `Cargo.toml`（ワークスペースルート）が作成されている
- [ ] `app/core/`, `app/audio/`, `app/assets/`, `app/vampire-survivors/` の4クレートが定義されている
- [ ] 各クレートの `Cargo.toml` と `src/lib.rs`（または `main.rs`）が作成されている
- [ ] `cargo build` が成功する
- [ ] `cargo run -p vs` でウィンドウが開く

**実装ガイド**:

```toml
# Cargo.toml（workspace）
[workspace]
resolver = "2"
members = [
    "app/core",
    "app/audio",
    "app/assets",
    "app/vampire-survivors",
]

[workspace.dependencies]
bevy = "0.15"
bevy_kira_audio = "0.21"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
```

```rust
// app/vampire-survivors/src/main.rs
use bevy::prelude::*;

fn main() {
    App::new()
        .add_plugins(DefaultPlugins.set(WindowPlugin {
            primary_window: Some(Window {
                title: "Vampire Survivors Clone".into(),
                resolution: (1280.0, 720.0).into(),
                ..default()
            }),
            ..default()
        }))
        .run();
}
```

---

### タスク 1.2: ディレクトリ構造の作成

**優先度**: P0
**推定工数**: 0.5時間
**ラベル**: task, phase-1

**説明**:
アセット・セーブデータ用のディレクトリ構造を作成する。

**受け入れ基準**:
- [ ] `assets/sprites/`, `assets/sounds/`, `assets/fonts/` が作成されている
- [ ] `save/` ディレクトリが作成されている
- [ ] `.gitignore` に `/target`, `/save/` が設定されている

---

### タスク 1.3: rust-toolchain.toml の作成

**優先度**: P1
**推定工数**: 0.25時間
**ラベル**: task, phase-1

**説明**:
Rustツールチェーンバージョンを固定する。

**受け入れ基準**:
- [ ] `rust-toolchain.toml` が作成されている
- [ ] stableチャンネルが指定されている

```toml
# rust-toolchain.toml
[toolchain]
channel = "stable"
```

---

### タスク 1.4: justfile の作成

**優先度**: P1
**推定工数**: 0.5時間
**ラベル**: task, phase-1, tooling

**説明**:
開発で頻繁に使うコマンドをjustfileに定義する。

**受け入れ基準**:
- [ ] `just run` でゲームが実行される
- [ ] `just check` でフォーマットとClippyが実行される
- [ ] `just test` でテストが実行される
- [ ] `just build` でリリースビルドが実行される

```just
# justfile
default:
    just run

run:
    cargo run -p vs

dev:
    cargo run -p vs --features bevy/dynamic_linking

check:
    cargo fmt --all -- --check
    cargo clippy --all -- -D warnings

test:
    cargo test --all

build:
    cargo build -p vs --release
```

---

### タスク 1.5: 設計ドキュメントの確認

**優先度**: P0
**推定工数**: 0.25時間
**ラベル**: task, phase-1, documentation

**説明**:
全設計ドキュメントが作成されていることを確認する。

**受け入れ基準**:
- [ ] `docs/01_specification.md` が存在する
- [ ] `docs/02_architecture.md` が存在する
- [ ] `docs/03_gameplay_systems.md` が存在する
- [ ] `docs/04_ui_ux.md` が存在する
- [ ] `docs/05_audio.md` が存在する
- [ ] `docs/06_implementation_plan.md` が存在する
- [ ] `docs/07_project_structure.md` が存在する
- [ ] `docs/08_crate_architecture.md` が存在する
- [ ] `docs/09_quick_reference.md` が存在する
- [ ] `docs/roadmap/` に全17フェーズのファイルが存在する

---

## フェーズ検証

### 検証項目

- [ ] `cargo build` が成功する
- [ ] `cargo run -p vs` でウィンドウが開く（1280x720、タイトル「Vampire Survivors Clone」）
- [ ] 4クレートが正しく設定されている
- [ ] ディレクトリ構造が設計書通りに作成されている
- [ ] justfile が機能する

## 完了条件

- [ ] すべてのタスクが完了している
- [ ] ウィンドウが正しく開く
- [ ] 開発環境が整備されている

## 次のフェーズ

Phase 1 完了 → 次は **Phase 2: コアゲームループ** に進む
